% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/days_since_applied.R
\name{days_since_applied}
\alias{days_since_applied}
\title{Calculate days since each application was submitted}
\usage{
days_since_applied(
  df,
  date_col = "date_applied",
  status_col = "status",
  flag_after = 21,
  reference_date = Sys.Date()
)
}
\arguments{
\item{df}{A data frame containing job application data.}

\item{date_col}{Character. Name of the column containing application dates.
Must be of class \code{Date}. Default is \code{"date_applied"}.}

\item{status_col}{Character. Name of the status column. Default is \code{"status"}.}

\item{flag_after}{Numeric. Number of days after which an "Applied" application
is considered potentially ghosted. Default is \code{21}.}

\item{reference_date}{A \code{Date} object to calculate days from. Defaults to
\code{Sys.Date()}.}
}
\value{
The original data frame with two added columns: \code{days_waiting} and
\code{possibly_ghosted} (logical).
}
\description{
Computes the number of days between each application date and today (or a
reference date), and flags applications that have received no response
after a specified number of days.
}
\examples{
days_since_applied(jobs)
days_since_applied(jobs, flag_after = 14)

}
